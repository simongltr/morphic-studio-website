# Iteration log

One line per iteration: what changed and why. Newest at the top.

- **v16** — 2026-06-08 — **WIP** — section 01 sticky card stack: introduce a vertical stack of 5 service cards that pile up at the top of the viewport on scroll, then release together. CSS-only approach: each `.stack-card` is `position: sticky; top: 96px` with `transform: translateY(idx * 56px)` so all cards share the same sticky anchor (release synchronously) while rendering offset in an escalier. Cards are spaced ~50vh apart in flow so each one scrolls in naturally before colliding with the pile. Removed the previous `.stack-pinned` JS-driven implementation (caused a visible "reserve" group below + a stack at top, because cards were absolute-positioned with `translateY(110%)` of card height — still on screen at rest). **Known issue:** the last card (idx 4) doesn't reach the pile before the stack releases — needs more scroll room at the end (likely larger `padding-bottom` or rethinking the unstick timing). Otherwise the section behaves as intended.
- **v15** — 2026-06-05 — close the gap between the end-of-animation slogan and the first section without introducing a sliding white block. (1) During the final beat of the wash (0.85 → 1.0), the masthead migrates up from `top: 41%` to `top: 18%` so the slogan settles near the top of the viewport. (2) The first section is pulled up via `.section--first { margin-top: -60vh }`, scoping the offset to a single section so additional sections later fall into normal flow. (3) `.site { display: flow-root }` blocks the margin from collapsing into `.site`'s box, so only the section's *text* appears in the hero area — the `.site` white background stays at its natural Y 280vh and never slides up as a visible block. (4) Wash completes slightly earlier (0.7 → 0.9) so the room image is gone just before the slogan finishes its migration. Hero stage background switched to `#fafaf8` so any leak around the seam blends with the paper.
- **v14** — 2026-06-05 — switch the site header to a transparent bar with the white logo + "MORPHIC STUDIO" name on the left and WORK / ABOUT / SERVICES / CONTACT on the right (Quicksand, wide tracking). Drop the end-of-dolly logo and brand ornaments inside the hero (along with the line-1 translateX shift) — the slogan now stays centered all the way through. Update the slogan text to "We craft digital / experiences built to last", add a Quicksand uppercase subtitle "Websites, identities and digital systems for brands with long-term vision." below it, and reposition: masthead moves up to top 30%, couple drops to bottom -10% so the hats almost reach the subtitle.
- **v13** — 2026-06-04 — make the cinematic transition feel like a real camera dolly: couple now also scales (1 → 4.5×, origin `50% 65%`) so we appear to walk past them, blur ramps earlier (DOF), and they fade out during the pass (0.35–0.7). Background scale increased to 3.0, stage lengthened to 280vh desktop / 220vh mobile for a slower, more cinematic move. Mouse parallax decays twice as fast (`1 - p × 2`) so it disengages before the couple grows. Title and wash timings shifted slightly later to fit the longer arc.
- **v12** — 2026-06-04 — scroll-driven cinematic transition from the dark hero into the light-paper site. A 150vh `.hero-stage` wraps a `position: sticky` hero with four layers (bg / couple / title / wash overlay). Scroll progress drives five CSS vars: bg `scale 1 → 2.6` from the bright center, couple `blur 0 → 16px` + opacity fade, title fades late during the wash, and a `#fafaf8` overlay washes in 0.65–1.0 to land on the v9 paper color. Below the stage, a stub `01 — Le constat` section confirms the seam. Mouse parallax from v11 is preserved and decays with progress. Mobile shortens the stage to 130vh and caps blur at 8px; `prefers-reduced-motion` collapses the cinematic entirely. New helper `screenshot-scroll.ts` captures progress frames 0/25/50/75/100.
- **v11** — 2026-06-04 — add a subtle mouse-driven parallax to the hero: background image stays fixed, while the title (~6/4px max) and couple (~14/8px max) drift toward the cursor. rAF + lerp smoothing, disabled on touch and `prefers-reduced-motion`. Also dropped `mix-blend-mode: multiply` in v10 once we confirmed the couple WebP already has a transparent background.
- **v10** — 2026-06-04 — restart from scratch with a full-page hero only: dark room background image, couple silhouette anchored bottom-center, and the headline "We craft digital / experiences that last" set in Libre Baskerville sitting between the two. Plain HTML + CSS, no Tailwind yet.
- **v9** — 2026-06-01 — adapt the design *to* the immutable logo instead of recoloring it: drop the `.logo-mark--light` modifier entirely and flip the **footer to a light background** so the logo renders with its real navy + coral + ochre + sage. Remove the **dark-blue band** from the launch section: it now sits on the paper section with a paper-2 callout card and a thin coral top accent. Fill out the empty-feeling stat cards in **hero**, **about** and **tarifs** with Iconify icons (`lucide:zap`/`sparkles`/`badge-euro`/`phone-call` in the hero, `compass`/`map-pin`/`calendar` in about, `file-text`/`package`/`eye-off` in pricing) + a short caption line under each label. Same treatment for the **cas d'usage** cards: each card gets a colored icon chip (globe, refresh, rocket, trending-up, shopping-bag, layers) and a heavier eyebrow (12px, font-semibold) so `01 / Site` reads with weight.
- **v8** — 2026-06-01 — use the real `logo.svg` from the repo root: inlined into both header and footer with a `--light` variant that swaps the navy fills for paper so the mark stays legible on the dark navy footer. Strip em dashes from CONTENT.md and the site: section/eyebrow separators became `/` (e.g. `03 / Services`, `01 / Site`); body em dashes turned into commas, colons or periods per context (`PME — soignés` → `PME. Soignés`). Drop the `max-w-[56ch]` on the inclus and pricing notes so they sit on a single line at desktop widths. Tailwind output recompiled.
- **v7** — 2026-06-01 — large iteration: switch to **Tailwind v4** compiled locally with `bunx @tailwindcss/cli` (theme tokens for logo colors, fonts, breakpoints declared in `input.css`; compiled to `output.css`, committed). Drop Space Grotesk — **everything is Inter** now (display uses Inter at weight 700, tighter tracking). Strip the **italic** out of heading highlights — only the color remains, and the highlight color cycles through the **logo palette** (coral / ochre / sage) instead of always coral. Remove the **services progress bar**. Anchor the horizontal-scroll end position by computing `lastCard.right - (pin.width - paddingRight)` in JS so the last card always lands at the body's right edge regardless of viewport. Replace the inclus checklist with a 4-column **Iconify icon card grid** (`lucide:` set, served via the `iconify-icon` web component CDN) — each item has a colored icon chip + title + short caption. CLAUDE.md updated with the Tailwind build step.
- **v6** — 2026-06-01 — structure pass: cards where information should be isolated, fewer ambient horizontal rules. Fixed the large gap between the services heading and the cards by moving `.section-head` *inside* `.services-pin` and stacking head + strip in a flex column (the head was outside the sticky pin in v5, so cards centered in 100vh ended up half a viewport below the heading). Cas d'usage bullet-list → 3-col card grid with a category eyebrow per card. Méthode steps → stacked cards (no `border-top` between them). Inclus checklist → single container card, no inner hairlines. Tarifs price table and FAQ accordion each wrapped in a container card so their dividers stay contained. Hero-meta, about-meta, pricing-points: replaced their `border-top` + grid with small paper-2 tile cards. Net effect: section separators are the only ambient horizontal rules left.
- **v5** — 2026-06-01 — align services strip with body content edge and tighten spacing everywhere. The strip's left/right padding now uses `max(var(--gutter), calc((100vw - var(--max-w)) / 2 + var(--gutter)))`, so cards enter and exit at the same x-position as the rest of the page's content (vs. edge-to-edge in v4). All vertical rhythm chopped: section padding `180px→104px`, hero `200px→120px`, launch `200px→120px`, final-cta `220px→130px`, footer `120px→80px`. Section-head margin-bottom `96px→56px`. Step padding `56px→32px`, FAQ summary `26px→18px`, included-grid li `18px→13px`, price-row `26px→18px`. Display sizes trimmed (h1 `5.5rem→4.4rem`, h2 `4.5rem→3.2rem`). Service cards smaller, less padded.
- **v4** — 2026-06-01 — fix the services pinned horizontal scroll asymmetry. v3 had a small left padding but `padding-right: 40vw` on the strip, so the first card entered at the left edge while the last card ended centered — visually unbalanced. Right padding now matches the left (`clamp(20px, 5vw, 64px)`), so the scroll travels edge-to-edge: card 1 sits at the left at start, card 3 sits at the right at end, card 2 centered mid-scroll.
- **v3** — 2026-05-31 — drop the two-column layout. Every section now stacks marker + heading + body full-width, restoring proper visual hierarchy (headings dominate, list items are clearly subordinate). Services section converted to a pinned horizontal scroll: vertical scroll pauses, three cards slide through, then scroll resumes. New helper `screenshot-services-scroll.ts` captures the three card states. Bullet-list typography reduced; pricing/included/FAQ given full content width. Mobile gracefully falls back to a vertical card stack.
- **v2** — 2026-05-31 — type and palette pivot per user feedback. Display swapped from Instrument Serif to **Space Grotesk** (500), body from Hanken Grotesk to **Inter**. Background lifted from warm paper (#f4ede0) to near-white (#fafaf8). Italic emphasis kept but now via sans italic + coral. Roman service/step numerals (i., ii., iii.) replaced with large coral display numerals (01, 02, 03). Display sizes reduced ~15% to accommodate the heavier sans grotesque.
- **v1** — 2026-05-31 — initial design pass. Editorial direction (Instrument Serif + Hanken Grotesk + JetBrains Mono on warm paper #f4ede0, logo colors as sparse accents). Full 12-section site rendered as single-file HTML with embedded CSS. Numbered section markers, asymmetric two-col layout, dark-navy launch block for rhythm. French typography fixed (nbsp before ?/:/!, non-breaking hyphen in "Parlons-en").
